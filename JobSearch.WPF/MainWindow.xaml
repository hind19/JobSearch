<Window x:Class="JobSearch.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:JobSearch.WPF.ViewModels"
        xmlns:views="clr-namespace:JobSearch.WPF.Views"
        mc:Ignorable="d"
        Title="JobSearch"
        Height="800"
        Width="960"
        MinHeight="600"
        MinWidth="780">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:UserProfileViewModel}">
            <views:UserProfileView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:JobSitesViewModel}">
            <views:JobSitesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EmailSettingsViewModel}">
            <views:EmailSettingsView />
        </DataTemplate>
    </Window.Resources>

    <ContentControl Content="{Binding CurrentViewModel}" />

</Window>