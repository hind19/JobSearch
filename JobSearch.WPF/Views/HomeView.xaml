<UserControl x:Class="JobSearch.WPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid VerticalAlignment="Center"
          HorizontalAlignment="Center"
          Width="520">
        <StackPanel>

            <TextBlock Text="{DynamicResource Home_Title}"
                       FontSize="22"
                       FontWeight="Medium"
                       Foreground="#212121"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,6" />

            <TextBlock Text="{DynamicResource Home_Subtitle}"
                       FontSize="14"
                       Foreground="#757575"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,32" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Анализ CV -->
                <Border Grid.Column="0"
                        Style="{StaticResource CardStyle}"
                        Margin="0">
                    <StackPanel>
                        <TextBlock Text="📄"
                                   FontSize="28"
                                   Margin="0,0,0,12" />
                        <TextBlock Text="{DynamicResource Home_CvCard_Title}"
                                   FontSize="16"
                                   FontWeight="Medium"
                                   Foreground="#212121"
                                   Margin="0,0,0,8" />
                        <TextBlock Text="{DynamicResource Home_CvCard_Description}"
                                   TextWrapping="Wrap"
                                   FontSize="13"
                                   Foreground="#757575"
                                   LineHeight="20"
                                   Margin="0,0,0,20" />
                        <Button Content="{DynamicResource Common_Open}"
                                Command="{Binding OpenProfileCommand}"
                                Style="{StaticResource OutlineButtonStyle}"
                                Padding="0,10" />
                    </StackPanel>
                </Border>

                <!-- Сайты поиска -->
                <Border Grid.Column="2"
                        Style="{StaticResource CardStyle}"
                        Margin="0">
                    <StackPanel>
                        <TextBlock Text="🌐"
                                   FontSize="28"
                                   Margin="0,0,0,12" />
                        <TextBlock Text="{DynamicResource Home_JobSitesCard_Title}"
                                   FontSize="16"
                                   FontWeight="Medium"
                                   Foreground="#212121"
                                   Margin="0,0,0,8" />
                        <TextBlock Text="{DynamicResource Home_JobSitesCard_Description}"
                                   TextWrapping="Wrap"
                                   FontSize="13"
                                   Foreground="#757575"
                                   LineHeight="20"
                                   Margin="0,0,0,20" />
                        <Button Content="{DynamicResource Common_Open}"
                                Command="{Binding OpenJobSitesCommand}"
                                Style="{StaticResource OutlineButtonStyle}"
                                Padding="0,10" />
                    </StackPanel>
                </Border>

            </Grid>
        </StackPanel>
    </Grid>

</UserControl>
