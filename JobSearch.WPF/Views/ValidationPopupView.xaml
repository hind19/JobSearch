<UserControl x:Class="JobSearch.WPF.Views.ValidationPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI"
             FontSize="13">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid>
        <Rectangle Fill="#80000000" />

        <Border Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24"
                MaxWidth="560"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="32">

            <StackPanel>

                <!-- Header -->
                <Grid Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource JobSites_Popup_Title}"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Margin="0,0,6,0" />
                        <TextBlock Text="{Binding EditingItem.Name}"
                                   FontSize="15"
                                   FontWeight="SemiBold" />
                    </StackPanel>
                    <Button Content="✕"
                            Command="{Binding CloseValidationPopupCommand}"
                            Style="{StaticResource OutlineButtonStyle}"
                            Padding="8,4"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center" />
                </Grid>

                <!-- Result message -->
                <Border CornerRadius="6"
                        Padding="12,8"
                        Margin="0,0,0,16">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background"
                                    Value="#E8F5E9" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ValidationLinks.Count}"
                                             Value="0">
                                    <Setter Property="Background"
                                            Value="#FFF3E0" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding ValidationMessage}"
                               FontSize="13"
                               TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground"
                                        Value="#2E7D32" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ValidationLinks.Count}"
                                                 Value="0">
                                        <Setter Property="Foreground"
                                                Value="#E65100" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Border>

                <!-- Links list -->
                <ScrollViewer MaxHeight="280"
                              VerticalScrollBarVisibility="Auto"
                              Margin="0,0,0,16"
                              Visibility="{Binding ValidationLinks.Count,
                                  Converter={StaticResource BoolToVis}}">
                    <ItemsControl ItemsSource="{Binding ValidationLinks}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="10,8"
                                        Margin="0,0,0,6"
                                        Background="#FAFAFA">
                                    <TextBlock Text="{Binding}"
                                               FontFamily="Consolas"
                                               FontSize="11"
                                               Foreground="#1565C0"
                                               TextWrapping="Wrap" />
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Close button -->
                <Button Content="{DynamicResource JobSites_Popup_Close}"
                        Command="{Binding CloseValidationPopupCommand}"
                        Style="{StaticResource OutlineButtonStyle}"
                        Padding="20,8"
                        HorizontalAlignment="Right" />

            </StackPanel>
        </Border>
    </Grid>
</UserControl>